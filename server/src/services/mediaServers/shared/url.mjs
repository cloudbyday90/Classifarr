import urlHelpers from './url.shared.js';

const {
  normalizeBaseUrl,
  normalizeResourcePath,
  buildPathUrl,
  appendQueryParam,
} = urlHelpers;

export {
  normalizeBaseUrl,
  normalizeResourcePath,
  buildPathUrl,
  appendQueryParam,
};

export default urlHelpers;
